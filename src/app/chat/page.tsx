import ChatWindow from '@/components/ChatWindow';

export const metadata = {
  title: 'Chat — HiveMinds',
  description: '5 AIs discuss your question and reach a consensus',
};

export default function ChatPage() {
  return <ChatWindow />;
}
